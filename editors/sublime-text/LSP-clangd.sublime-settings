// Settings in here override those in "LSP-clangd/LSP-clangd.sublime-settings"

{
  "initialization_options": {
    "fallbackFlags": [
      "-I${folder}/include",
      "-I${folder}/lib",
      "-I${folder}/tests"
    ]
  }
}
