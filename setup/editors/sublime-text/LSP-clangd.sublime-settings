// Settings in here override those in "LSP-clangd/LSP-clangd.sublime-settings"

{
  "initializationOptions": {
    "fallbackFlags": [
      "-I${folder}/include",
      "-I${folder}/lib",
      "-I${folder}/tests"
    ]
  }
}
